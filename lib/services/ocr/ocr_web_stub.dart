class OCRWeb {
  static String createBlobUrl(List<int> bytes) {
    throw UnsupportedError(
      'createBlobUrl nie jest obsługiwane na tej platformie',
    );
  }

  Future<Map<String, dynamic>> runOcr(String imagePath) async {
    throw UnsupportedError('OCRWeb nie jest obsługiwane na tej platformie');
  }
}
